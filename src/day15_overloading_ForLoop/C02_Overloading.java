package day15_overloading_ForLoop;

public class C02_Overloading {
    public static void main(String[] args) {
        /*java da oluşturduğumuz metodların isminin yaptığı işler
        ile uyumlu olmasını isteriz.mesela bir Stringin bir bölümünü
        almak için java 2 adet substring() metodları veya
        verilen stringdeki bazı parçaları yenileri ile değiştirmek için 2 adet replace()
        metodu var.
        java aynı isimde birden fazla metod varsa hangisinin kullanılacağına
        parametre sayısı ve parametrelerin data türüne göre karar verir

         */


        //aynı isimde ama farklı metodları oluşturmak için
        //değiştirebileceğimiz şeyler
        //1- parametre sayısı
        //2- aynı sayıda parametre olsabile parametrelerin data türlerini değiştirebiliriz
        //3- aynı sayıda ve aynı data türünde parametreleri olan metodlarda
        // parametrelerin sırası değişebilir
    }
}
